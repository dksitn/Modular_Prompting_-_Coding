# 《初始化架構操作手冊》

*(Initialization Manual for Modular AI System – GitHub × VS Code)*

---

## 一、目標說明

本手冊的目的在於，
讓整個 **模組化方法論（Code + GEM + Layer + Project）**
能以 **GitHub 為版本核心**、**VS Code 為操作介面** 的方式落地運行。

---

## 二、系統層級概覽

| 層級            | 功能角色          | 主要儲存位置          | 管理重點      |
| ------------- | ------------- | --------------- | --------- |
| **Core 層**    | 程式與提示詞的穩定邏輯基底 | `core/`         | 模組化與版本控制  |
| **Layer 層**   | 框架與組裝邏輯       | `dev/layers/`   | 統一開發流程與模板 |
| **Project 層** | 實際專案應用與輸出     | `dev/projects/` | 專案配置、組裝成品 |
| **Docs 層**    | 方法論與規範文件      | `docs/`         | 文件、方法論、教學 |

---

## 三、在 GitHub 建立倉庫

### 🧱 建立主結構

在 GitHub 建立一個新專案（例如）：

```
modular-ai-system
```

clone 到本地：

```bash
git clone https://github.com/<your-org>/modular-ai-system.git
cd modular-ai-system
```

---

## 四、建立資料夾結構

```bash
mkdir -p core/code
mkdir -p core/prompts/{components,gems,registry,tools}
mkdir -p dev/layers/{foundation,logic,interface,tools}
mkdir -p dev/projects/proj-demo/{app,prompts/{recipes,compiled_prompts,evals},ssot,tests}
mkdir -p docs/methods
touch README.md
```

---

## 五、每層說明與初始化

### 🧩 1️⃣ Core 層

> 儲存基礎邏輯（Code 模組、GEM 模組）

```
core/
 ├─ code/
 │   └─ example_service.py
 └─ prompts/
     ├─ components/
     │   └─ fn_analyze_intent.md
     ├─ gems/
     │   └─ gem_knowledge_extractor.json
     ├─ registry/
     │   └─ index.json
     └─ tools/
         ├─ build_prompt.py
         └─ prompt_preview.py
```

`example_service.py`

```python
class ExampleService:
    def process(self, text):
        return f"Processed: {text}"
```

---

### ⚙️ 2️⃣ Layer 層

> 封裝 Core 功能成開發框架與共用工具

```
dev/layers/
 ├─ foundation/
 │   └─ schema.json
 ├─ logic/
 │   └─ pipeline_base.py
 ├─ interface/
 │   └─ layer_adapter.py
 └─ tools/
     └─ build_code.py
```

`pipeline_base.py`

```python
from core.code.example_service import ExampleService

class PipelineBase:
    def run(self, text):
        svc = ExampleService()
        return svc.process(text)
```

---

### 🧠 3️⃣ Project 層

> 代表具體應用專案，負責組裝與執行

```
dev/projects/proj-demo/
 ├─ app/
 │   ├─ main.py
 │   └─ build/
 ├─ prompts/
 │   ├─ recipes/spec_demo.json
 │   └─ compiled_prompts/
 ├─ ssot/schema.json
 └─ tests/
```

`app/main.py`

```python
from dev.layers.logic.pipeline_base import PipelineBase

if __name__ == "__main__":
    p = PipelineBase()
    print(p.run("Hello Modular AI!"))
```

`prompts/recipes/spec_demo.json`

```json
{
  "name": "spec_demo",
  "use_gems": [
    {"name": "gem_knowledge_extractor", "version": "1.0.0"}
  ]
}
```

---

### 📘 4️⃣ Docs 層

> 放置方法論與使用手冊（即你目前的這四份文件）

```
docs/methods/
 ├─ 01_code_methodology.md
 ├─ 02_gem_methodology.md
 ├─ 03_integration_methodology.md
 ├─ 04_project_methodology.md
 └─ init_manual.md   ← 就是這份手冊
```

---

## 六、VS Code 設定

在專案根目錄新增 `.vscode/` 目錄：

```
.vscode/
 ├─ settings.json
 ├─ tasks.json
 └─ launch.json
```

### `settings.json`

```json
{
  "python.defaultInterpreterPath": "python3",
  "json.schemas": [
    {
      "fileMatch": ["core/prompts/gems/*.json"],
      "url": "./schemas/gem_schema.json"
    }
  ]
}
```

### `tasks.json`

```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "🔧 Build Prompt",
      "type": "shell",
      "command": "python core/prompts/tools/build_prompt.py --recipe ${workspaceFolder}/dev/projects/proj-demo/prompts/recipes/spec_demo.json",
      "group": "build"
    },
    {
      "label": "🚀 Run Demo App",
      "type": "shell",
      "command": "python dev/projects/proj-demo/app/main.py",
      "group": "test"
    }
  ]
}
```

### `launch.json`

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "🧩 Debug Demo App",
      "type": "python",
      "request": "launch",
      "program": "dev/projects/proj-demo/app/main.py"
    }
  ]
}
```

---

## 七、Git 初始化與上傳

```bash
git init
git add .
git commit -m "chore: initialize modular ai system"
git branch -M main
git remote add origin https://github.com/<your-org>/modular-ai-system.git
git push -u origin main
```

---

## 八、檢查與測試

### ✅ 檢查 Code 層運作

```bash
python dev/projects/proj-demo/app/main.py
```

輸出應為：

```
Processed: Hello Modular AI!
```

### ✅ 組裝 Prompt

```bash
python core/prompts/tools/build_prompt.py --recipe dev/projects/proj-demo/prompts/recipes/spec_demo.json
```

輸出：

```
✅ Compiled prompt written to dev/projects/proj-demo/prompts/compiled_prompts/spec_demo.md
```

---

## 九、未來擴充方向

| 功能           | 對應位置                       | 說明       |
| ------------ | -------------------------- | -------- |
| 新增 GEM       | `core/prompts/gems/`       | 版本化組合模組  |
| 新增 Component | `core/prompts/components/` | 單一功能模組   |
| 新增 Project   | `dev/projects/proj-*`      | 建立專案模板複製 |
| 新增 Layer 邏輯  | `dev/layers/logic/`        | 定義新開發框架  |
| 新增方法論文件      | `docs/methods/`            | 擴充組織規範   |

---

## 🧠 十、操作心法

> **GitHub 是後端版本治理，VS Code 是前端操作介面。**
> 兩者串聯後，你就擁有了一個完整的「模組化 AI 工程系統」：

* 所有邏輯都有明確歸屬
* 所有模組皆可版本回溯
* 所有組裝皆可自動化
* 所有專案皆可複用與擴展

---

## ✅ 總結

| 層級      | 任務                  | 操作位置             |
| ------- | ------------------- | ---------------- |
| Core    | 開發與維護模組（Code / GEM） | GitHub + VS Code |
| Layer   | 定義開發邏輯、封裝流程         | VS Code          |
| Project | 組裝與測試、產生成品          | VS Code Terminal |
| Docs    | 管理方法論、記錄規範          | Markdown 文件庫     |

---

> 💡 **一句話總結**：
> 在 GitHub 建好結構，在 VS Code 裡操作與組裝。
> GitHub 是「腦幹」，VS Code 是「神經系統」。
> 兩者連起來，整個 AI 方法論就能真正「活起來」。

---

是否希望我幫你接著生成一份
📗《第一個專案初始化範例（proj-demo）操作指南》？
它會詳細示範從空專案 → 建 GEM → 組 Prompt → 執行 → 驗證 的完整步驟。
